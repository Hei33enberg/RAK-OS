{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://rak.ad/schemas/kinds/review.ext.schema.json",
  "title": "FOP `review` ext (market profile, L1)",
  "description": "Per-kind bag for a FOP review. FOP keeps `rating` at the atom top level in its frozen wire core; as a base-envelope atom it maps to `ext.rating` (see docs/profiles/fop.md). Either location is accepted.",
  "type": "object",
  "properties": {
    "rating": { "type": ["integer", "null"], "minimum": 1, "maximum": 5, "description": "1..5 or null (qualitative)." }
  }
}
